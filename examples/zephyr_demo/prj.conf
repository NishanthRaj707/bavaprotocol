# Zephyr RTOS Configuration for BAVA Protocol Demo Application

# Enable Kernel & Console output
CONFIG_STDOUT_CONSOLE=y
CONFIG_PRINTK=y

# Enable UART / Serial Driver support
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y

# Enable BAVA Protocol Integration
CONFIG_BAVA_PROTOCOL=y
